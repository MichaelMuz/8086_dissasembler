const disassemble = @import("disassembler/disassemble.zig");
const instructions = @import("disassembler/instructions.zig");
const operands = @import("disassembler/operands.zig");

test {
    _ = disassemble;
    _ = instructions;
    _ = operands;
}
